# pylint: disable=missing-docstring
# this file is needed in order to run "coverage run" successfully.

from kiosk.kiosk import main

if __name__ == '__main__':
    # execute only if run as the entry point into the program
    main()
